// This file was auto-generated by Fern from our API Definition.

/**
 * A recoverable error raised while processing the turn.
 */
export interface ErrorStreamEvent {
    /** Monotonic sequence number; echo the last seen value via Last-Event-ID to resume. */
    seq: number;
    /** Error payload (message and, when available, a status code). */
    data: Record<string, unknown>;
}
