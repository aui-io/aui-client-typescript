// This file was auto-generated by Fern from our API Definition.

/**
 * A streaming event emitted while the agent works (token deltas, stream end).
 */
export interface TokenStreamEvent {
    /** Monotonic sequence number; echo the last seen value via Last-Event-ID to resume. */
    seq: number;
    /** Event payload; token deltas carry the text under data.text. */
    data: Record<string, unknown>;
}
