// This file was auto-generated by Fern from our API Definition.

import type * as Apollo from "../index.js";

/**
 * An agent version — an immutable-once-published config snapshot.
 */
export interface AgentVersion {
    /** Version id */
    id: string;
    /** Owning agent id */
    agent_id: string;
    version_number: number;
    version_revision_number: number;
    /** Canonical tag, e.g. 'v3.2' */
    version_tag: string;
    status: AgentVersion.Status;
    /** Lineage: cloned from */
    parent_version_id?: string;
    label?: string;
    tags?: string[];
    notes?: string;
    stats?: Apollo.VersionStats;
    /** Runtime build this version's bundle targets, e.g. '0.8.0'. None when the bundle pins no runtime */
    runtime_version?: string;
    /** AUI schema version of this version's bundle — the 'schema_version' it was pushed with. None until a bundle is pushed */
    schema_version?: string;
    created_at: string;
    published_at?: string;
}

export namespace AgentVersion {
    export const Status = {
        Draft: "draft",
        Published: "published",
        Archived: "archived",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
